#lang htdp/isl+

#| Exercise 421.
Is (bundle '("a" "b" "c") 0) a proper use of the bundle function?
What does it produce? Why?
|#

; it's not a proper use, never end, drop always produce same result with input