#lang htdp/bsl

(define-struct child [parents dob date])
; Since child, parents, dob, and date are variables and the parentheses are placed according to the grammatical pattern


; (define-struct oops [])
; (define-struct (child person) [dob date])
; define-struct is not followed by a single variable name and a sequence of variables in parentheses.